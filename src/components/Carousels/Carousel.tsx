import React, { PropsWithChildren } from "react";

interface IProps { }

export const Carousel = (props: PropsWithChildren<IProps>) => {

    const NextItem = () => {

    }

    const PreviousItem = () => {

    }

    return (
        <div className={"carousel"}>

        </div>
    );
}
